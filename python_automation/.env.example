# Copy this file to .env in python_automation/ and fill in values for your lab.
# .env is gitignored (see root .gitignore) so your credentials never land in
# the public repo.

NET_USERNAME=CHANGEME
NET_PASSWORD=CHANGEME
